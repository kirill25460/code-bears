import styled from 'styled-components';

export const HeadFromConteiner = styled.div`
  margin-bottom: 110px;
`;

export const HeadFromMain = styled.p`
  font-family: 'DM Sans';
  font-style: normal;
  font-weight: 400;
  font-size: 30px;
  line-height: 104%;
  text-align: center;
  letter-spacing: -0.02em;

  color: #ffffff;
`;

export const HeadFromSpan = styled.p`
  font-family: 'DM Sans';
  font-style: normal;
  font-weight: 700;
  font-size: 30px;
  line-height: 104%;
  text-align: center;
  letter-spacing: -0.02em;

  mix-blend-mode: hard-light;

  color: #9365ff;
`;
